<body class="body-forms">
<h1 class="header">Add Picture</h1>
<h4 class="sub-title">~ Add a picture for <%= @city.name %> ~</h4>
<hr>
<br>

<div class="form-wrapper">

  <form action="/add-city-picture/<%= @city.id %>/<%= @city.name %>/<%= @city.visited %>/<%= @city.country_id %>" method="post">
    <div>
      <label for="picture">Enter picture URL for <%= @city.name %> (Landscape will work best) </label>
      <br>
      <input type="text" name="picture"/>
    </div>

    <div>
      <input type="submit" value="Save City Photo"/>
    </div>

  </form>
</div>
