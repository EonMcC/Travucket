<body class="body-forms">
<h1 class="header">Add Picture</h1>
<h4 class="sub-title">~ Add a picture for <%= @country.name %> ~</h4>
<hr>
<br>

<div class="form-wrapper">

  <form action="/add-country-picture/<%= @country.id %>/<%= @country.name %>/<%= @country.visited %>" method="post">
    <div>
      <label for="picture">Enter picture URL for <%= @country.name %> (Landscape will work best) </label>
      <br>
      <input type="text" name="picture"/>
    </div>

    <div>
      <input type="submit" value="Save Photo"/>
    </div>

  </form>
</div>
